{{#each menuItems as |item|}}
  <SideNavMenuItem @item={{item}} @activeCategoryId={{this.activeCategoryId}}/>
{{/each}}
<PaperButton onClick={{action 'invalidateSession'}} @noink={{true}} @raised={{false}} class="menu-item">
  <div class="item-name">Logout</div>
</PaperButton>
