<PaperButton @noink={{true}} @raised={{false}} class="menu-item">
  <div class="item-name" role='button' onClick={{action 'redirectTo' @item.name}}>{{@item.name}}</div>
  <div class="sub">{{concat @item.numberOfPosts ' posts'}}</div>
  <PaperDivider class="divider-light" />
</PaperButton>
